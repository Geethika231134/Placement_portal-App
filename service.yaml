apiVersion: v1
kind: Service
metadata:
  name: placement-service
spec:
  type: NodePort
  selector:
    app: placement-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30007